# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*_kustomization_backup.yaml
plan.txt
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.tfvars
*.tfvars.json

# Kubeconfig fetched locally for debugging — never commit cluster credentials
kubeconfig*.yaml

# JetBrains IDE
.idea/
